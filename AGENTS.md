# 项目开发规则

本项目遵循 `AGENT.md` 的全局规则，重点约束如下。

## 项目定位
- 本项目是“警情智能报告”系统，不是通用若依后台。
- 旧若依项目仅作为业务参考，不直接复制框架结构。
- 目标是轻量、可维护、可验证、高度定制化。

## 架构原则
- 后端采用 Router -> Service -> Repository 分层。
- 前端采用 View -> Store/Composable -> API Client -> Component 分层。
- 报告内容以结构化 JSON 作为权威数据，HTML/Docx/PDF 是派生产物。
- AI 默认只生成草稿，必须经过校验和人工确认后保存。
- SQL 必须参数绑定，禁止拼接用户输入。
- 敏感配置只进入 `.env` 或环境变量，禁止提交真实密钥。

## 第一阶段最小闭环
1. 登录/当前用户占位能力。
2. 警情查询与统计接口骨架。
3. 报告新建、生成草稿、保存、查看接口骨架。
4. 前端报告工作台、警情查询、报告编辑页面骨架。
5. 健康检查、启动脚本、基础验证命令。

## 验证命令
- 后端：`python -m compileall -q backend/app backend/tests`
- 前端：`npm run build`
- 通用：`git diff --check`
