# 全局开发规则与多 Agent 协作手册

> 用途：这是一个跨项目通用的开发起始规则文件。新项目启动时，可以先复制本文件到仓库根目录，并根据项目实际情况改名为 `AGENTS.md`、`PROJECT_RULES.md` 或 `DEVELOPMENT_PLAYBOOK.md`。
> 原则：本文件只写通用工程方法，不写任何特定项目、特定业务、特定表名、特定端口、特定仓库、特定历史功能。

---

## 1. 总体工作原则

### 1.1 先理解，再行动

任何开发任务开始前，先明确：

1. 用户真正想解决什么问题；
2. 当前系统已有能力是什么；
3. 这次改动的最小闭环是什么；
4. 哪些行为必须保持兼容；
5. 哪些操作存在安全、数据、性能或稳定性风险；
6. 是否需要先问清楚，而不是直接假设。

禁止在没有理解上下文时直接大面积修改。

---

### 1.2 小步快跑，原子变更

每次改动应尽量只解决一个明确问题。

推荐节奏：

```text
小方案 -> 小改动 -> 快验证 -> 提交 -> 再迭代
```

避免：

```text
一次性重写多个模块
一个提交混合功能、重构、格式化、依赖升级
为了解决小问题引入大框架
```

---

### 1.3 优先闭环，不追求一次完美

第一版优先保证：

```text
能配置
能执行
能看到结果
能看到错误
能回滚或修正
```

高级能力可以后续迭代，例如：

```text
自动化
智能化
批量化
可视化增强
性能优化
多租户
复杂权限
```

不要因为追求完整设计而迟迟没有可用版本。

---

### 1.4 稳定性优先级高于炫技

技术选择优先级：

```text
正确性 > 数据安全 > 可维护性 > 可观测性 > 性能 > 开发速度 > 技术新颖度
```

不为了炫技引入不必要的复杂度。

---

## 2. 模块化与架构边界

### 2.1 单一职责

每个模块、文件、函数应只有一个主要变化原因。

避免一个文件同时负责：

```text
路由
业务逻辑
数据库访问
外部 API 调用
UI 状态
日志处理
定时任务
权限判断
```

---

### 2.2 推荐后端分层

通用后端分层建议：

```text
Router / Controller
  -> Application Service
  -> Domain Service / Infrastructure
  -> Repository / ORM / Database / External Client
```

规则：

1. Router 只处理 HTTP、鉴权依赖、参数接收、响应返回；
2. 业务编排放到 Service；
3. 数据访问放到 Repository、ORM 或 Infrastructure；
4. 下层不能反向依赖上层；
5. Service 不应依赖具体 Router；
6. 外部系统调用应封装，不能散落在业务代码里。

---

### 2.3 推荐前端分层

通用前端分层建议：

```text
View / Page
  -> Composable / Hook / Store
  -> API Client
  -> Presentational Component
```

规则：

1. View 负责页面布局和组合；
2. API 请求集中到 API 模块；
3. 业务状态和动作进入 Composable、Hook 或 Store；
4. 展示组件通过 props / emits / callbacks 通信；
5. 展示组件不要直接发业务请求；
6. 表单编辑应使用副本，避免未保存污染原始数据。

---

### 2.4 保持最小公开接口

模块只暴露调用方真正需要的内容。

避免：

```text
跨模块调用私有函数
为了方便导出整个内部对象
把内部数据结构暴露给 UI 或外部接口
```

---

## 3. 兼容性规则

### 3.1 默认保持兼容

除非用户明确要求破坏兼容，否则应保持：

```text
接口路径兼容
请求字段兼容
响应字段兼容
错误语义兼容
历史数据兼容
用户操作流程兼容
```

---

### 3.2 破坏性变更必须说明

如果必须破坏兼容，必须提前说明：

```text
破坏点是什么
影响哪些用户或模块
为什么必须破坏
如何迁移
如何回滚
```

---

### 3.3 兼容门面可以临时保留

重构时可以保留旧入口作为兼容门面。

例如：

```text
旧模块只做 re-export
旧接口内部调用新 Service
旧字段保留但标记 deprecated
```

等调用方迁移完成后再删除。

---

## 4. 数据库与数据安全规则

### 4.1 明确权威数据源

任何业务数据、配置数据、任务状态、审计数据都应明确权威存储位置。

禁止让关键业务状态只存在于：

```text
浏览器本地存储
进程内存
临时文件
短期缓存
日志文本
```

缓存可以用于加速，但不能替代权威存储。

---

### 4.2 SQL 必须安全构造

用户输入必须通过参数绑定。

禁止：

```text
字符串拼接 SQL 值
把用户输入直接拼进 WHERE 条件
把未校验的表名或字段名拼进 SQL
```

要求：

```text
值使用绑定参数
表名和字段名使用白名单或严格标识符校验
危险语句默认禁止
```

---

### 4.3 高危数据库操作必须确认

以下操作默认禁止自动执行，除非用户明确确认：

```text
DROP
TRUNCATE
批量 DELETE
批量 UPDATE
覆盖生产表
删除历史数据
不可逆迁移
```

涉及数据破坏时，应提供：

```text
影响范围
备份建议
回滚方式
验证 SQL
```

---

### 4.4 大数据处理不能一次性加载

对于可能很大的数据集，禁止一次性读取或渲染全部数据。

推荐：

```text
分页
流式处理
游标
Keyset pagination
批处理
限制最大返回行数
后台任务
```

---

### 4.5 密钥与敏感信息保护

禁止把以下信息写入源码、日志、测试快照、文档或 Agent 文件：

```text
真实密码
Token
API Key
Secret
私钥
生产连接串
```

敏感信息应放在：

```text
.env
环境变量
凭据管理服务
受控配置中心
```

展示时必须脱敏。

---

## 5. API 与错误处理规则

### 5.1 API 错误要可理解

面向用户或前端的错误应清晰说明：

```text
哪里错了
为什么错
用户应该怎么处理
```

避免只返回底层异常栈。

---

### 5.2 底层错误要适度保留细节

对于排查有帮助的错误，可以保留必要细节，但不能泄露敏感信息。

推荐：

```text
中文友好提示 + 脱敏后的底层错误片段
```

---

### 5.3 接口契约要有测试或清单

关键接口至少验证：

```text
路径存在
方法正确
状态码正确
请求字段有效
响应结构稳定
错误语义符合预期
```

---

## 6. 前端交互规则

### 6.1 页面交互要符合用户心智

功能入口应放在用户自然会寻找的位置。

不要把“全局能力”藏在“局部配置”里。

例如：

```text
创建整体对象 -> 用独立助手或新建入口
编辑某个节点/字段 -> 放在该节点/字段配置里
查看全局日志 -> 放在全局菜单
查看本次执行日志 -> 放在执行结果区域
```

---

### 6.2 大列表必须分页或虚拟滚动

以下内容默认需要分页、截断或虚拟滚动：

```text
日志
执行记录
审计记录
搜索结果
数据预览
大表查询结果
消息列表
```

---

### 6.3 表单保存前不污染原始状态

编辑弹窗或抽屉应复制一份 form。

点击保存后再提交。

点击取消时不影响原始数据。

---

### 6.4 危险操作需要二次确认

例如：

```text
删除
覆盖
停止任务
清空配置
替换当前编辑内容
```

确认文案要说明后果。

---

## 7. AI 功能开发规则

### 7.1 AI 默认只生成草稿

AI 生成的内容默认是草稿，不能自动执行危险动作。

推荐流程：

```text
用户输入自然语言
-> AI 生成结构化草稿
-> 系统校验
-> 用户确认
-> 应用到当前编辑区
-> 用户手动保存或执行
```

---

### 7.2 AI 输出必须校验

不能直接信任 AI 输出。

必须校验：

```text
JSON 是否合法
字段是否在白名单内
SQL / 代码是否安全
引用资源是否存在
是否越权
是否包含敏感信息
是否符合项目约束
```

---

### 7.3 AI 要有可解释结果

AI 不应只给最终产物，还应给用户说明：

```text
它理解了什么
使用了哪些字段或资源
生成了什么逻辑
哪些地方需要用户确认
```

---

### 7.4 AI 失败要可恢复

AI 调用失败、超时、输出错误时，应允许用户：

```text
修改描述后重试
查看错误原因
使用模板兜底
手动编辑生成结果
```

---

### 7.5 AI 不能替代权限和安全控制

即使是 AI 生成，也必须经过同样的权限、校验、审计和安全策略。

---

## 8. 后台任务、同步与调度规则

### 8.1 长任务必须后台化

耗时任务不应让 HTTP 请求长期挂起。

推荐：

```text
创建任务记录
后台执行
前端轮询或订阅状态
记录执行日志
支持停止或取消
```

---

### 8.2 长任务必须可观测

至少记录：

```text
任务状态
开始时间
结束时间
处理数量
错误信息
关键执行步骤
必要的脱敏 SQL 或请求日志
```

---

### 8.3 长任务停止必须安全

停止任务时不要粗暴杀线程或进程，除非明确知道后果。

推荐：

```text
状态标记为 stopping
当前批次完成后停止
必要时中断底层阻塞请求
记录停止原因
保持 checkpoint 或进度一致
```

---

### 8.4 定时任务必须有并发保护

定时任务系统应考虑：

```text
同一任务重复触发
全局并发上限
单资源并发上限
失败重试
跳过策略
告警策略
```

默认不要无限排队堆积。

---

## 9. 测试与验证规则

### 9.1 优先测试关键契约

测试优先覆盖：

```text
接口契约
核心算法
权限边界
数据安全
错误处理
兼容行为
```

不是所有 UI 细节都必须第一时间写自动化测试，但必须有可执行回归清单。

---

### 9.2 每次完成要跑合适的验证

按技术栈选择验证命令。

通用检查：

```bash
git diff --check
git status --short
```

后端常见检查：

```bash
python -m pytest -q
python -m compileall -q app tests
```

前端常见检查：

```bash
npm run build
```

按项目实际情况替换为对应命令。

---

### 9.3 验证真实服务状态

有些问题测试能过，但运行服务没生效。

因此涉及后端路由、前端页面、启动脚本、热更新时，应额外验证：

```text
服务是否已重启
端口是否被旧进程占用
接口是否返回新行为
前端是否加载新构建或新 dev server
```

---

## 10. Git 与交付规则

### 10.1 提交要原子且可读

提交信息格式建议：

```text
feat: 增加某能力
fix: 修复某问题
refactor: 重构某模块
test: 增加某测试
chore: 调整工程配置
```

避免：

```text
update
fix
misc
临时提交
多个无关功能混在一个提交
```

---

### 10.2 提交前确认工作区

提交前：

```bash
git status --short
git diff --check
```

确认没有误提交：

```text
.env
密钥文件
构建产物
缓存文件
node_modules
__pycache__
日志文件
临时文件
```

---

### 10.3 Push 后确认远端一致

如果用户关心是否推送成功，应确认：

```bash
git ls-remote origin refs/heads/<branch>
git rev-parse HEAD
git rev-parse origin/<branch>
```

不要只看命令看似成功。

---

## 11. 热更新与本地服务规则

### 11.1 不要完全相信热更新

热更新可能因为以下原因失效：

```text
新增文件未被监听
新增 import 未触发 reload
旧子进程仍占端口
前端缓存
dev server 状态异常
文件系统事件丢失
```

遇到“代码已改但接口 404 / 页面没变”，优先确认运行进程和路由状态。

---

### 11.2 启停脚本要可靠

开发环境应提供：

```text
启动脚本
停止脚本
健康检查
日志位置
端口占用清理
```

如果服务有父子进程，停止时要处理整个进程树。

---

### 11.3 新增路由后建议重启验证

新增后端路由、启动配置、依赖、环境变量时，建议重启服务后验证。

---

## 12. 多 Agent 协作规则

### 12.1 主 Agent 负责最终判断

主 Agent 负责：

```text
理解需求
拆分任务
决定方案
协调子 Agent
审查结果
执行最终修改
运行验证
向用户交付总结
```

不能把“理解”和“最终责任”完全交给子 Agent。

---

### 12.2 子 Agent 适合处理的任务

适合交给子 Agent：

```text
大范围代码搜索
跨模块影响分析
复杂方案设计
独立风险评审
测试策略设计
安全审查
性能排查
文档归纳
```

不适合交给子 Agent：

```text
已知文件的一两行修改
简单读文件
简单 grep
无需上下文的小修补
```

---

### 12.3 常见 Agent 职责划分

可以按角色组织：

```text
Scout / Explore：快速定位文件、符号、调用链
Analyst：梳理模糊需求，输出结构化需求
Designer / Planner：设计实现方案和影响范围
Developer：按明确方案修改代码
Tester：补测试、跑验证、复现问题
Reviewer：审查安全、兼容性、架构风险
```

---

### 12.4 并行协作适用场景

适合并行：

```text
前端影响分析
后端影响分析
数据库影响分析
安全风险分析
测试覆盖分析
```

不适合并行：

```text
多个 Agent 同时修改同一文件
多个 Agent 同时改同一模块核心逻辑
需求还没明确就大量并发
```

---

### 12.5 子 Agent 结果必须验证

子 Agent 的输出只是参考，主 Agent 必须验证：

```text
实际 diff
测试结果
接口返回
构建结果
日志
运行状态
```

不要直接把子 Agent 的总结当成事实。

---

## 13. 推荐开发流程

### 13.1 普通任务流程

```text
1. 复述目标，确认边界
2. 阅读相关代码和测试
3. 制定最小实现方案
4. 修改代码
5. 补测试或回归清单
6. 运行验证
7. 检查 git diff
8. 提交并推送
9. 验证运行服务
10. 总结改动、验证结果、提交号
```

---

### 13.2 复杂任务流程

```text
1. 主 Agent 明确问题和约束
2. Scout 定位代码和调用链
3. Analyst 输出结构化需求
4. Designer 给出方案和风险
5. Developer 实现
6. Tester 验证
7. Reviewer 审查
8. 主 Agent 收口、修正、提交
```

可根据任务大小裁剪，不要机械使用所有角色。

---

### 13.3 Bug 排查流程

```text
1. 先复现
2. 看日志
3. 确认运行的是不是新代码
4. 定位入口
5. 缩小范围
6. 写最小修复
7. 加回归测试
8. 验证真实接口或页面
```

---

## 14. 新项目启动清单

新项目开始时，建议先完成：

```text
1. 写项目规则文件
2. 明确技术栈
3. 明确目录结构
4. 明确数据权威存储
5. 明确安全边界
6. 明确启动/停止方式
7. 明确测试命令
8. 建立健康检查
9. 建立日志位置
10. 建立提交规范
11. 建立敏感信息处理规则
12. 建立多 Agent 协作方式
```

---

## 15. 可复制的最小 AGENTS.md 模板

```markdown
# 项目开发规则

## 基本原则
- 先理解需求，再修改代码。
- 每次只做一个原子变更。
- 保持模块边界清晰。
- 默认保持兼容。
- 不把密钥写入源码、日志、测试快照或 Agent 文件。

## 后端规则
- Router / Controller 只处理协议层逻辑。
- 业务逻辑进入 Service。
- 数据访问进入 Repository / ORM / Infrastructure。
- SQL 参数必须绑定，标识符必须校验。
- 错误提示要清晰、可理解、不过度泄露敏感信息。

## 前端规则
- View / Page 只做布局和组合。
- API 请求进入 API Client。
- 业务状态进入 Composable / Hook / Store。
- 展示组件通过 props / emits / callbacks 通信。
- 大数据列表必须分页、截断或虚拟滚动。

## 数据与安全规则
- 明确权威数据源。
- 缓存不能替代权威存储。
- 不执行未经确认的破坏性操作。
- 敏感信息只允许进入环境变量或凭据管理系统。

## AI 规则
- AI 默认只生成草稿。
- AI 输出必须校验。
- 用户确认后才能应用。
- AI 不能绕过权限、安全和审计。

## 多 Agent 协作
- 主 Agent 负责理解、决策、验证和收口。
- 子 Agent 用于搜索、分析、设计、测试、审查。
- 不盲信子 Agent，必须检查实际 diff 和验证结果。

## 验证
- 提交前运行与改动范围匹配的测试、构建和格式检查。
- 修改运行服务相关内容后，必须验证真实服务状态。
```

---

## 16. 最重要的工作习惯

长期坚持：

```text
先理解
小步改
可验证
可回滚
有日志
有测试
不泄密
不盲信
不破坏兼容
交付说清楚
```
